import { test, expect } from '@playwright/test';

            test('test for deleteRowff in prvp.component.ts', async ({ page }) => {
            await page.goto('https://example.com');
            // Add more test steps here
        });


test('test for deleteRow3 in prvp.component.ts', async ({ page }) => {
            await page.goto('https://example.com');
            // Add more test steps here
        });