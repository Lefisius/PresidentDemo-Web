import { test, expect } from '@playwright/test';

            test('test for NAMEFUNC in state.component.ts', async ({ page }) => {
            await page.goto('https://example.com');
            // Add more test steps here
        });
test('test for NAMEFUNC1 in state.component.ts', async ({ page }) => {
            await page.goto('https://example.com');
            // Add more test steps here
        });
