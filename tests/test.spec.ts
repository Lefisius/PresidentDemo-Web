import { test, expect } from '@playwright/test';

        test('test for test.component.html', async ({ page }) => {
            await page.goto('https://example.com');
            // เขียนสคริปต์เพิ่มเติมที่นี่
        });
test('another test for test.component.ts', async ({ page }) => {
            await page.goto('https://example.com/another-page');
            // เขียนสคริปต์เพิ่มเติมที่นี่
        });